#include <iostream>
#include "Triangulo.h"
using namespace std;

int main() {
	Triangulo triangulito;
	triangulito.calculararea(5, 6);
	cout << "El area del triangulo es: " << triangulito.calculararea(5, 6);
	return 0;
}
